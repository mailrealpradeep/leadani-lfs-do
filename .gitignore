node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
test-results/
playwright-report/
playwright/.cache/
uploads/

# Local secrets — never commit.
.env
.env.*
!.env.example

# Local database dumps and restore reports. These are multi-GB production
# exports containing real customer data — never commit them.
db/dumps/*
!db/dumps/.gitkeep
.dev.env.sh
